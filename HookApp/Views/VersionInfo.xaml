<Window x:Class="HookApp.Views.VersionInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HookApp.Views"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="バージョン情報" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="txtVersionInfo"
                   Grid.Row="0"
                   Grid.Column="0"
                   Text="test"
                   Padding="10"
                   ></TextBlock>

        <TextBox x:Name="txtUpdateLog"
                 Grid.Row="1"
                 Grid.Column="0"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 Text="UpdateLogが入る予定。"
                 ></TextBox>
                 
        
    </Grid>
</Window>
